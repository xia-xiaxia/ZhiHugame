# 存档系统使用说明

## 功能概述

实现了完整的游戏存档系统，可以在游戏退出时自动保存，启动时自动加载。

## 文件说明

### 1. SaveData.cs
存档数据结构，包含：
- 游戏进度（年份、货币）
- 五维属性（君主、贵族、士族、外臣、国人）
- 阈值设置
- 政策背包
- BUFF背包
- 延时事件队列
- 已使用事件记录
- 存档时间戳

### 2. SaveManager.cs
存档管理器，提供：
- 自动保存/加载功能
- 手动保存/加载功能
- 删除存档功能
- 新游戏功能

## Unity 中的设置步骤

### 1. 创建 SaveManager GameObject

在游戏的初始场景（通常是开始界面场景）中：

1. 创建一个空的 GameObject，命名为 "SaveManager"
2. 添加 `SaveManager` 脚本组件
3. 在 Inspector 中配置：
   - **Stats**: 拖入你的 StatModel ScriptableObject
   - **Auto Save On Exit**: 勾选（退出时自动保存）
   - **Auto Load On Start**: 勾选（启动时自动加载）

### 2. 设置 DontDestroyOnLoad

SaveManager 会自动设置为 `DontDestroyOnLoad`，确保在场景切换时不被销毁。

### 3. 存档文件位置

存档文件保存在：
```
Application.persistentDataPath/savegame.json
```

不同平台的路径：
- **Windows**: `C:/Users/用户名/AppData/LocalLow/公司名/游戏名/savegame.json`
- **Mac**: `~/Library/Application Support/公司名/游戏名/savegame.json`
- **Android**: `/storage/emulated/0/Android/data/包名/files/savegame.json`
- **iOS**: `/var/mobile/Containers/Data/Application/应用ID/Documents/savegame.json`

## 使用方法

### 自动保存和加载

启用 `autoSaveOnExit` 和 `autoLoadOnStart` 后，系统会自动：
- 游戏启动时加载存档（如果存在）
- 游戏退出时保存当前进度
- 移动平台暂停时保存

### 手动保存和加载

在代码中调用：
```csharp
// 手动保存
SaveManager.Instance.SaveGame();

// 手动加载
SaveManager.Instance.LoadGame();

// 检查是否存在存档
bool hasSave = SaveManager.Instance.HasSaveFile();

// 获取存档信息（不完全加载）
SaveData info = SaveManager.Instance.GetSaveInfo();

// 删除存档
SaveManager.Instance.DeleteSave();

// 开始新游戏（重置数据）
SaveManager.Instance.NewGame();
```

### UI 按钮调用

可以在 UI 按钮的 OnClick 事件中绑定：
- `SaveManager.ManualSave()` - 手动保存
- `SaveManager.ManualLoad()` - 手动加载
- `SaveManager.NewGame()` - 新游戏

## 保存的数据内容

存档包含以下内容：

### 基础数据
- 当前年份
- 货币数量
- 五维属性值（君主、贵族、士族、外臣、国人）
- 五维阈值（最小值和最大值）

### 背包数据
- **政策背包**：包含所有道具的完整信息
  - 道具ID、类型、名称、描述
  - 效果说明、数值变化
  - 阈值变化、免死情况
  - 使用次数、价格等
  
- **BUFF背包**：包含所有时局的完整信息
  - BUFF ID、名称、描述
  - 持续时间、剩余年数
  - 每年的数值变化

### 事件数据
- **延时事件队列**：所有计划在未来触发的事件
- **已使用事件**：防止事件重复出现
- **激活事件集**：当前可用的事件集索引

### 元数据
- 存档时间戳
- 游戏版本号

## 测试方法

### 1. 测试自动保存
1. 运行游戏，玩一段时间
2. 退出游戏
3. 检查存档文件是否生成
4. 再次运行游戏，检查是否恢复到之前的状态

### 2. 查看存档内容
打开存档文件 `savegame.json`，内容类似：
```json
{
  "year": 5,
  "currency": 100,
  "king": 55,
  "noble": 48,
  "scholar": 52,
  "foreign": 50,
  "people": 53,
  "saveTime": "2025-10-30 15:30:45",
  "gameVersion": "1.0.0"
}
```

### 3. 测试手动保存
在游戏中添加保存按钮：
1. 创建 UI Button
2. 在 OnClick 事件中添加 `SaveManager.ManualSave()`
3. 点击按钮测试

## 注意事项

1. **StatModel 必须绑定**：SaveManager 需要引用 StatModel 才能工作
2. **场景切换**：SaveManager 会自动在场景间保持，不需要在每个场景都创建
3. **存档兼容性**：如果修改了数据结构，旧存档可能无法加载
4. **异常处理**：所有保存/加载操作都有异常捕获，不会导致游戏崩溃
5. **调试日志**：所有操作都有详细的日志输出，方便调试

## 扩展功能（可选）

### 多存档槽
如果需要多个存档槽，可以修改 `SAVE_FILE_NAME`：
```csharp
private const string SAVE_FILE_NAME = "savegame_{0}.json"; // {0} 为槽位编号
```

### 云存档
可以在 SaveManager 中添加云存档功能，上传到服务器或云服务。

### 存档加密
可以在保存前加密 JSON 字符串，防止玩家修改存档。

## 常见问题

### Q: 存档文件在哪里？
A: 在 Unity Editor 中运行时，按 F1 键打开 Console，查看日志输出的存档路径。

### Q: 如何删除存档？
A: 可以手动删除存档文件，或调用 `SaveManager.Instance.DeleteSave()`。

### Q: 如何测试新游戏？
A: 删除存档文件后重新运行游戏，或调用 `SaveManager.Instance.NewGame()`。

### Q: 存档失败怎么办？
A: 检查 Console 日志，通常是因为 StatModel 未绑定或文件权限问题。
